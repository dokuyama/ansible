---
# ----------------------------------------
# CentOS 7.x settings
# ----------------------------------------
bastion_os_user: ec2-user
qw_os_user: ec2-user
lk_os_user: ec2-user

# RedHat 7.8 (x86_64)
ami_owners: 309956199498
ami_search_string: "RHEL-7.8_HVM_GA-*"

bastion_host_ami_id: "{{ ami_find.image_id }}"
active_host_ami_id: "{{ ami_find.image_id }}"
qw_host_ami_id: "{{ ami_find.image_id }}"

